package com.furever.webapplication.FurEver.config;

/**
 * CORS is now centrally managed in SecurityConfig.corsConfigurationSource()
 * This file is kept for reference but is no longer used.
 * All CORS origins are configured via app.cors.origins property in application.properties
 */
@Deprecated
public class CorsConfig {
}